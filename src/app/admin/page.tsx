"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import "./admin.css";
export default function AdminLogin() { const router = useRouter(); const [error, setError] = useState(""); const [busy, setBusy] = useState(false); async function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); setBusy(true); setError(""); const body = Object.fromEntries(new FormData(e.currentTarget)); const r = await fetch("/api/auth/login", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(body) }); const data = await r.json(); setBusy(false); if (!r.ok) { setError(data.error || "Connexion impossible"); return; } router.push("/admin/dashboard"); }
 return <main className="admin-shell"><section className="admin-login-card"><span className="admin-kicker">FINAB · ESPACE PARTENAIRE</span><h1>Votre tableau de bord</h1><p>Connectez-vous pour retrouver les demandes de rendez-vous et gérer les inscrits.</p><form onSubmit={submit}><label>Adresse e-mail ou numéro WhatsApp<input name="login" required autoComplete="username" placeholder="E-mail ou WhatsApp" /></label><label>Mot de passe<input name="password" required type="password" autoComplete="current-password" placeholder="Votre mot de passe" /></label>{error && <div className="admin-error">{error}</div>}<button disabled={busy} type="submit">{busy ? "Connexion..." : "Se connecter"}</button></form><a href="/opportunite-assurance">← Retour à la page publique</a></section></main>; }
